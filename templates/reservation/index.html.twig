{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		<div class="mt-5">
			<h1 class="main-titles text-center mb-5">Reservation</h1>
			<p class="mb-5">Lorem, ipsum dolor sit amet consectetur adipisicing elit. Aperiam sint praesentium veniam nesciunt vero, iure maxime mollitia optio dolores a!</p>
		</div>

		{# IF USER IS CONNECTED  #}
		{% if app.user %}
			<div class="wrapper wrapper-form bg-light text-black contact">
				<div class="row p-5">
					<div
						class="col-12 col-md-7 px-5">
						{# FORM  #}
						{{ form_start(form, { 'attr': { 'id': 'reservationForm' } }) }}
						<div class="row d-flex">
							<div class="col-12 col-lg-6">{{ form_row(form.firstname) }}
							</div>

							<div class="col-12 col-lg-6">{{ form_row(form.lastname) }}</div>
							<div class="col-12 col-lg-6">{{ form_row(form.telephone) }}</div>
							<div class="col-12 col-lg-6">{{ form_row(form.numberOfPeople) }}</div>
							<div class="col-12 col-lg-6">{{ form_row(form.reservationDate) }}</div>
							<div class="col-12 col-lg-6 text-black py-0">{{ form_row(form.reservationTime) }}</div>
							<div class="col-12 ">{{ form_row(form.comments) }}</div>
						</div>
						<div class="text-center">
							<button type="submit" class="btn btn-primary btn-link my-4 text-capitalize w-100">Send</button>
						</div>
						{{ form_end(form) }}

					</div>
					<div class="col-12 col-md-5 px-5">
						<div class="d-flex flex-wrap flex-column align-items-center">
							<p class="fs-3 fw-medium">Tasty Drink Bar & Shop</p>
							<div class="contact-phone my-1">
								<i class="fa-solid fa-phone"></i>
								<span>555-554545</span>
							</div>
							<div class="contact-email my-1">
								<i class="fa-solid fa-envelope"></i>
								<span>contact@tastydrink.com</span>
							</div>
						</div>
						<div class=" mx-auto my-5">
							<img class="img-fluid" src="{{ asset('assets/images/tasty_drink.jpeg') }}" alt="tasty drink building"/>
						</div>
						<div class="mt-3 text-center">
							<p class="m-0 fs-3 fw-medium">2345 George Vancouver</p>
							<p class="m-0 fs-3 fw-medium">V5K0A3</p>
							<p class="m-0 fs-3 fw-medium">Vancouver</p>
							<p class="m-0 fs-3 fw-medium">BC Canada</p>
						</div>
					</div>
				</div>
			</div>

			{# USER NOT CONNECTED  #}
		{% else %}
			<div class="wrapper wrapper-form bg-light text-black contact">
				<div class="text-center w-100 p-5 text-dark fw-medium bg-white border border-secondary rounded py-4 px-5 my-5">

					<h2>You need to be connected to make a reservation.</h2>
					<p class="mt-5">
						<a href="{{path('app_login')}}" class="text-dark text-decoration-underline">Click here to login</a>
					</p>
				</div>
			</div>
		{% endif %}
	</div>

{% endblock %}
